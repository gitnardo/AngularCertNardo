import { CardLoaderDirective } from './card-loader.directive';

describe('CardLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CardLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
